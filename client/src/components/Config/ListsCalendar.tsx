import { Box } from '@mui/material'
import React from 'react'
import { Layout } from '../Layout'

export const ListsCalendar = () => {
  return (
    <Layout>
      <Box sx={{ border: '2px dashed green' }}>List Calendar</Box>
    </Layout>
  )
}
