import { useRouteError } from 'react-router-dom'
import { styled, css, Box } from '@mui/material'

export const ErrorPage = styled(() => {
  return <Box>This is Error Page</Box>
})(({ theme }) => {
  return css``
})
